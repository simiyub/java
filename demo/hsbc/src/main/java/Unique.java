
/**
 *
 * Write a program to tell us whether an input String contains all unique characters.
 * For example, the String HSBC does have all unique characters but the String hello does not because there are two ls.
 * */


public interface Unique {

boolean    isUnique(String string);
}
