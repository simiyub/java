package binarysearch;

/**
 * Given an integer, return its index in a given array or -1 if it is not in the array.
 * */
public interface BinarySearch {

    int indexOf(int[] array, int requiredNumber);
}
