import util.BST;
import util.Node;

/**
 * Implement the logic for summing depths here
 * */
public class SumOfBSTDepthsImpl implements SumOfBSTDepths {
    @Override
    public int depths(Node tree) {
        return 0;
    }
}
