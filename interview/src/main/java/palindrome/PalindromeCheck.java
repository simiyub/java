package palindrome;

/**
 * The function will take in a non-empty string and return a boolean
 * of whether the string is a palindrome or not.
 * */
public interface PalindromeCheck{
    boolean isPalindrome(String string);
}
