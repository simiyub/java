package sumofbstbranches;

import util.BST;

import java.util.List;

/**
 * Walk through the branches of a BST and sum up the value of each branch
 * and return an array of the sums.
 * */
public interface SumOfBSTBranches {
     List<Integer> sumOfBranches(BST tree);
}
