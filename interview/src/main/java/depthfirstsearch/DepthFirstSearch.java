package depthfirstsearch;
import util.BST;
import java.util.List;

/**
 * This function takes a list of node object and returns an array representing
 * the names of the nodes retrieved in a depth first search manner.
 * */
public interface DepthFirstSearch{

    List<Integer> list (BST tree);
}
