package arrayofproducts;

/**
 * This function will receive an array of integers and
 * for each index position, it will return the product of all other the numbers
 * in the array.
 * */
public interface ArrayOfProducts {
    int[] products(int[] array);
}
