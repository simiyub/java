/**
 *This function takes a two dimension array of integers
 * and returns a one dimensional array of all the elements in spiral order.
 **/
public interface SpiralTraverse {
    int[] traverse(int[][] array);
}
