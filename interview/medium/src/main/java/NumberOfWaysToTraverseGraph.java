/**
 * This function receives dimensions of a graph and
 * determines how many ways we can traverse a graph with these dimensions.
 * */
public interface NumberOfWaysToTraverseGraph {
    int count(int width, int height);
}
