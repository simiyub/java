/**
 * This function will receive a tree and
 * two strings representing the start and end nodes and will return
 * the path between the start and end strings.
 * */
public interface PathBetweenTwoNodes {
    String findPath(String start, String end);
}
