package arrays;
/**
 * This function takes a non-empty array of distinct integers and a target sum
 * and returns an array of any two distinct numbers in the array that sum up to the target
 * or an empty array if none exist
 * **/
public interface TwoNumberSum {
    int[] findSum(int[] array, int target);
}
