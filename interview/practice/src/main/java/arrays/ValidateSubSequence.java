package arrays;

/**
 * Given two non-empty arrays, this function will return true if the second array is a subsequence
 * of the first array or not.
 * **/

public interface ValidateSubSequence {
    boolean isSubSequence(int[] array, int[] potentialSubSequence);
}
